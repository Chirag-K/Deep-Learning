Problem Statement : 
We have a dataset with 10,000 images of cats and dogs. We are Predicting 
whether the given image image is of cat or dog.

We split the data in 80:20 for training and validation. 
Pre-Processing of data is done with ImageDataGenerator.
Then, CNN is used to train the model.